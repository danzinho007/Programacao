// #include <iostream>
// Biblioteca de entrada e saída
// Inclui a biblioteca padrão de entrada e saída do C++.
// Ela contém o objeto std::cout (para imprimir na tela) e 
// std::cin (para ler do teclado).
// iostream” = input/output stream (fluxo de entrada/saída)

// #include <iomanip> 
// para controlar casas decimais

// #include <string>
// Armazena o nome do vendedor (não é usado na saída, mas é lido conforme o enunciado).
// using namespace std;
// Evita ter que usar std:: antes de cout e cin

// #include <cmath>
// Biblioteca de potência

// int main()
// É a função principal do programa.
// Todo programa em C++ começa sua execução aqui.
// O int significa que a função retorna um número inteiro 
// (geralmente 0 para indicar que terminou sem erro).

// {
// Abre o bloco de código da função main.
// Tudo entre { e } pertence a essa função.

    // std::cout << "Hello World!" << std::endl;
    // Usar isso quando não se coloca using namespace std;
    // std::cout → é o objeto de saída padrão (standard 
    // output), ou seja, a tela.
    // << → é o operador de inserção, que envia o que vem 
    // depois para a saída.
    // "Hello World!" → é o texto literal a ser exibido.
    // std::endl → adiciona uma quebra de linha (\n) e 
    // limpa o buffer de saída.
    // (serve para garantir que o texto apareça 
    // imediatamente no console)
    // ; → indica o fim da instrução.
    
    // cout <<"Pulando linha\nTeste";
    // Outra forma de pular linha
    
    // int A, B;        
    // declara duas variáveis inteiras
    
    // cin >> A;        
    // lê o primeiro valor (A)

    // cin >> A >> B;
    // Lê dos 2 valores em seguidas
    
    // int X = A + B;   
    // calcula a soma
    
    // cout << "X = " << X << endl; 
    // imprime no formato exigido
    
    // double R;                  
    // raio
    
    // cin >> R;                  
    // lê o raio
    
    // double A = 3.14159 * R * R; 
    // calcula a área
    
    // cout << fixed << setprecision(4); 
    // define 4 casas decimais
    // cout << "A=" << A << endl;       
    // imprime no formato exigido
    
    // system("pause");
    // Caso dê erro > sh 1: pause: not found
    // Remover o system("pause")

    // return 0;
    // Encerra o programa e retorna o valor 0 para o 
    // sistema operacional.
    // Por convenção, 0 significa que o programa terminou 
    // com sucesso, sem erros.
    
// }
// Fecha o bloco iniciado pelo {.
// Indica fim da função main e, portanto, o fim do programa.

// Erros
// Main.cs(1,0): error CS1525: Unexpected symbol `include'
// Acontece porque você está tentando executar código C++ 
// em um compilador de C#.


// Main.cpp:9:11: error: invalid operands of types 
// ‘<unresolved overloaded function type>’ and 
// ‘const char [8]// ’ to binary ‘operator<<’
//normalmente acontece quando o compilador não 
// reconhece cout ou você misturou std e using 
// namespace std de forma incorreta, ou ainda 
// quando há algum erro de digitação.
// Exemplo count ao invés de cout 

// Main.cpp:8:17: error: expected initializer before ‘>>’ 
// token
// Colocando >> ao invés de =

// Main.cpp:10:27: error: ‘MEDIA’ was not declared in this
// scope
// Media não foi declarada

Pesos 2, 3 e 5
5 * 2 = 10
6 * 3 = 18
7 * 5 = 35
35 + 28 = 63
63  | 3
010 | 2.3
  1 | 

#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    int cod1, qtd1, cod2, qtd2;
    double valor1, valor2;

    // lê os dados do produto 1
    cin >> cod1 >> qtd1 >> valor1;
    // lê os dados do produto 2
    cin >> cod2 >> qtd2 >> valor2;

    double total = qtd1 * valor1 + qtd2 * valor2; // calcula o valor total

    cout << fixed << setprecision(2); // 2 casas decimais
    cout << "VALOR A PAGAR: R$ " << total << endl;

    return 0;
}

#include <iostream>
#include <iomanip>
using namespace std;

int main() {
    double A, B, C;
    cin >> A >> B >> C;

    double triangulo = (A * C) / 2.0;
    double circulo = 3.14159 * C * C;
    double trapezio = (A + B) * C / 2.0;
    double quadrado = B * B;
    double retangulo = A * B;

    cout << fixed << setprecision(3);
    cout << "TRIANGULO: " << triangulo << endl;
    cout << "CIRCULO: " << circulo << endl;
    cout << "TRAPEZIO: " << trapezio << endl;
    cout << "QUADRADO: " << quadrado << endl;
    cout << "RETANGULO: " << retangulo << endl;

    return 0;
}

#include <iostream>
#include <iomanip>
#include <cmath>  // para pow()
using namespace std;

int main() {
    double A, B, C;
    cin >> A >> B >> C;

    double triangulo = (A * C) / 2.0;
    double circulo = 3.14159 * pow(C, 2);       // usando pow()
    double trapezio = (A + B) * C / 2.0;
    double quadrado = pow(B, 2);                // usando pow()
    double retangulo = A * B;                   // poderia usar pow(A,1)*pow(B,1) mas é desnecessário

    cout << fixed << setprecision(3);
    cout << "TRIANGULO: " << triangulo << endl;
    cout << "CIRCULO: " << circulo << endl;
    cout << "TRAPEZIO: " << trapezio << endl;
    cout << "QUADRADO: " << quadrado << endl;
    cout << "RETANGULO: " << retangulo << endl;

    return 0;
}

#include <iostream>
#include <cmath>
using namespace std;
int main() {
    int a, b, c;
    cin >> a >> b >> c;
    int MaiorAB = (a + b + abs(a - b)) / 2;  
    int Maior = (MaiorAB + c + abs(MaiorAB - c)) / 2;
    cout << Maior << " eh o maior" << endl;
    return 0;
}
Q1015
#include <iostream>
#include <cmath>
#include <iomanip>
using namespace std;

int main() {
    double x1, y1, x2, y2;
    cin >> x1 >> y1 >> x2 >> y2;

    double distancia = sqrt(pow(x2 - x1, 2) + pow(y2 - y1, 2));

    cout << fixed << setprecision(4);
    cout << distancia << endl;

    return 0;
}



#include <iostream>
#include <algorithm>
using namespace std;
int main() {
    int a, b, c;
    cin >> a >> b >> c;
    cout << max({a, b, c}) << " eh o maior" << endl;
}

---------------------------------------
Q1016


60---1hora---X   ---2horas---X
90-----------x+30------------y+60

Hora = distância
1 = 30
2 = 60

// carros x e y
// x vel 60 km/h
// y vel 90 km/h
// 1hr > y = 30 + x
// 2hr > y = 60 + x
#include <iostream>
#include <iomanip>
#include <cmath>
#include <string>
using namespace std;
int main(){
    double x,y;
    int z;
    cin >> z;
    int min = z * 2;
    cout << min << " minutos" << endl;
}

---------------------------------
Q1017


// Quantidade de litros de combustível gasto = float
// Automóvel 12 km/l
// Tempo gasto em horas = int
// Velocidade média km/h = int
// Tempo gasto 10 horas com 85 km/h
// Km - l
// 85 - x
// 12 - 10 > 850 / 12 > 70,833
#include <iostream>
#include <iomanip>
using namespace std;
int main(){
    int t, v;
    cin >> t >> v;
    double litros = t * v / 12.0;
    cout << fixed << setprecision(3);
    cout << litros << endl;
    return 0;
}

-------------------
Q1018
// 576 / 100 = 5 notas de 100
sobra 76
// 76 / 50 = 1 nota de 50
sobra 26

#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;

    cout << N << endl;

    int notas100 = N / 100;
    N = N % 100;

    int notas50 = N / 50;
    N = N % 50;

    int notas20 = N / 20;
    N = N % 20;

    int notas10 = N / 10;
    N = N % 10;

    int notas5 = N / 5;
    N = N % 5;

    int notas2 = N / 2;
    N = N % 2;

    int notas1 = N / 1;
    N = N % 1;

    cout << notas100 << " nota(s) de R$ 100,00" << endl;
    cout << notas50  << " nota(s) de R$ 50,00"  << endl;
    cout << notas20  << " nota(s) de R$ 20,00"  << endl;
    cout << notas10  << " nota(s) de R$ 10,00"  << endl;
    cout << notas5   << " nota(s) de R$ 5,00"   << endl;
    cout << notas2   << " nota(s) de R$ 2,00"   << endl;
    cout << notas1   << " nota(s) de R$ 1,00"   << endl;

    return 0;
}

------------------------
Q1019


#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;

    int horas = N / 3600;
    N = N % 3600;

    int minutos = N / 60;
    int segundos = N % 60;

    cout << horas << ":" << minutos << ":" << segundos << endl;
    return 0;
}


---------------------
Uma tarefa simples! Você receberá os pontos finais de alguns segmentos de linha. Basta desenhá-los! Está achando muito fácil, certo?

Você decide desafiar a si mesmo e, portanto, adicionar algumas restrições.
1] Iniciar a partir do ponto final de qualquer segmento de linha.
2] Não levante a caneta do papel.
3] Você pode passar por cima de uma linha de mais de uma vez.
4] Não desenhar qualquer outra linha além das citadas.
5] Você só pode mudar de direção em coordenadas inteiras de x e y.
6] Use a menor quantidade de tinta necessária.

   1|2|3|4|5
A |X| | | | 
B | | |X| | A1-C2
C | |X|X|X| B3-C3
D | |X| | | C4-C2
E | | | | | C2-D2
Você precisa de uma unidade de tinta para desenhar uma unidade de linha. As distâncias entre os pontos sucessivos
são: 1 unidade.

Portanto Distância (A1 até B1) = 1 unidade and Distância (A1 até A2) = 1 unidade.

Entrada
A primeira linha de entrada é um inteiro T(T<20) que indica o número total de casos de teste. Cada caso inicia com um inteiro N (N<10). Então N linhas vem a seguir, cada uma descrevendo um segmento de linha. Um segmento de linha é definido por dois pontos Y1X1 Y2X2. Y1 e Y2 estarão no intervalo [A,E]. X1 e X2 estarão no intervalo [1,5]. O primeiro exemplo é mostrado na figura acima. Portanto, olhe para o exemplo para compreender o formato exato.

Saída
Para cada caso, imprima o número do caso de teste seguido pela quantidade total de tinta requerida com 2 dígitos depois do ponto decimal. Se as linhas não podem ser desenhadas sem que se levante a caneta, imprima o número do caso de teste seguido de “~ x (“, conforme exemplo abaixo (caso 2).

Exemplo de Entrada	Exemplo de Saída
2                       Case 1: 8.24 	
4                       Case 2: ~x(
A1 C2
B3 C3
C4 C2
C2 D2
2
A1 A5
E1 E5
